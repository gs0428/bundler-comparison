{
  "name": "esbuild",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/main.js --bundle --outfile=dist/bundle.js",
    "visualize": "esbuild src/main.js --bundle --metafile=dist/meta.json --outfile=dist/meta.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.20.2"
  },
  "dependencies": {
    "lodash": "^4.17.21"
  }
}
